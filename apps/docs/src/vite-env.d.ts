/// <reference types="vite/client" />

/* For `import src from '...?url'` in the preview stories: Vite's client types declare the
 * `?url` suffix, and without this reference `tsc` sees an unresolvable module specifier. */
